import { createMuiTheme } from "@material-ui/core";

const theme = createMuiTheme({
  palette: {
    primary: {
      light: "#fff565",
      main: "#e9dc2f",
      dark: "#6e6816",
    },
    secondary: {
      light: "#696969",
      main: "#292929",
      dark: "#000000",
    },
  },
});

export default theme;
